//use local storage to store access and refresh token in browser, so the following const are keys to access the access token and refresh token
export const ACCESS_TOKEN = "access"
export const REFRESH_TOKEN = "refresh"